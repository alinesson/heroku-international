<%# suppressing outut of supplemental menu except for where we explicitly call it %>
<%= render '/refinery/content_page', :hide_sections => [:supplemental_menu, :lead_story, :sub_a, :sub_b, :sub_c] %>